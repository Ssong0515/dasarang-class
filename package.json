{
  "name": "dasarang-class",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "predev": "node scripts/generate-slide-manifest.cjs",
    "dev": "astro dev --host 0.0.0.0 --port 8000",
    "prebuild": "node scripts/generate-slide-manifest.cjs",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port 8001"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^12.6.13",
    "astro": "^5.18.0",
    "wrangler": "^4.38.0"
  }
}
